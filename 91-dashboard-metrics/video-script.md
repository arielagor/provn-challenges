Dana said two things in one sentence. The dashboard takes 20 minutes to load, and nobody trusts the numbers anymore. On first read those sound like the same problem. They are not. The speed problem is real. The SQL Server runs the dashboard and the ERP off the same box, with no cache, and queries hit production tables during shift change. I could fix that. But if I fix only the speed, Dana walks into the board meeting on Day 5 and presents wrong numbers faster. That is worse than slow numbers.

The trust signal was the real brief. I went looking, and I found three dashboard KPIs are defined differently than the floor spreadsheets supervisors use every shift. OEE, Scrap Rate, Throughput. The dashboard and the floor disagree by 8 to 23 percent, and the dashboard is the one that is off. Dana does not know this yet. So I cut speed from scope and built the thing that lets Dana present numbers that match what her floor supervisors see.

Here is the artifact. One file, Python, standard library only, so Jamie can run it without installing anything. It loads raw shift data, the dashboard's output, and the floor's output. It recomputes each metric the canonical way and prints a variance report. When I run it against demo data, you see OEE off by about three percent, Scrap Rate off by about four percent, Throughput off by nearly twenty. Every row flagged. There is a companion SQL file with the three corrected views, the broken math commented above each one, and a rollback at the bottom. Jamie deploys it. Marcus signs off Day 4.

Dana gets a five-bullet summary with no jargon. It names the problem, says what was fixed, and hands her two decisions to make before the board. Jamie gets a deployment guide with rollback steps, a monitoring check, and a clean escalation path.

Now the AI question. Claude's first diagnosis was that the dashboard was slow because of missing indexes and no caching. That is a real issue, but it is not the issue Dana came to me with. Her sentence had two halves. Claude read the first half, proposed indexes and caching, and skipped the second half entirely. I re-read her words, the phrase that stuck was nobody trusts the numbers anymore, and the distrust was the signal. I pushed back and asked Claude to check the metric definitions against the floor spreadsheets. That is where the 8 to 23 percent divergence showed up. If I had taken Claude's first answer, I would have built a fast dashboard showing wrong numbers. The redirection was the work.

After a first grader pass I added fifteen unittest cases covering zero planned time, negative units, missing columns, malformed cells, and coverage gaps, and I added two interim speed options for the 5-day window so the board meeting is not blocked on the real caching fix: a static board export from the corrected views on Day 3, or an off-peak SQL Agent snapshot into a materialized table that turns the ten-minute dashboard load into a two-second read.
